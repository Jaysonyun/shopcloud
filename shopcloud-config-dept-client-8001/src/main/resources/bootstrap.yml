spring:
  cloud:
    config:
      name: shopcloud-config-dept-client      #定义要读取资源文件的名称
      profile: dev  #定义profile的名称  beta
      label: master   #定义程序的配置文件所在分支
      uri: http://config-7101.com:7101  #SpringCloudConfig的服务地址
      username: shopjava
      password: hello